#include "PmergeMe.hpp"

//Ford-Johnson algorithm (also called merge-insertion sort algorithm)
void	fj_vector(const char **arr)
{
	//fill vector
	
}

void	PmergeMe(const char **arr)
{
	print_array(explicit message); //template function
	get_time();
	//do algorithm with vector
	get_time();
	get_time();
	//do algorithm with list
	get_time();
	print_array(explicit message); //sorted	
}